// C O N V E R S I O N S

#include <iostream>
#include <math.h>
using namespace std;
int main()
{
    // DECIMAL TO BINARY CONVERSION

    // int n;
    // cout << "Enter the Number you Want to convert\n";
    // cin >> n;
    // int i = 0;
    // int answer = 0;
    // while (n != 0)
    // {
    //     int bit = n & 1;
    //     n = n >> 1;
    //     answer = (bit * pow(10, i)) + answer;
    //     i++;
    // }
    // cout << "Binary Conversion of n is "<< answer;

    // BINARY TO DECIMAL CONVERSION

    // int n;
    // cout << "Enter the Number you Want to convert\n";
    // cin >> n;
    // int i = 0;
    // int answer = 0;
    // while (n != 0)
    // {
    //     int digit = n % 10;
    //     n = n / 10;
    //     if (digit == 1)
    //     {
    //         answer = answer + pow(2, i);
    //     }

    //     i++;
    // }
    // cout << "Decimal Conversion is " << answer;

    // COMPLIMENT OF A NUMBER

    // int n;
    // cout << "Enter the Number you Want to convert\n";
    // cin >> n;
    // int m = n;
    // int mask;
    // while (m != 0)
    // {
    //     m = m >> 1;
    //     mask = (mask << 1) | 1;
    // }
    // int ans = mask & ~n;
    // cout << "Compliment of " << n << " is " << ans << endl;

    // REVERSE THE INTEGER AND IF THE VALUE IF OUT OH THE TNTEGER RANGE THEN RETURN O

    // int n;
    // cout << "Enter the Number you Want to convert\n";
    // cin >> n;
    // int i = 0;
    // int ans = 0;
    // while (n != 0)
    // {
    //     int digit = n % 10;

    //     ans = (ans * 10) + digit;
    //     n = n / 10;
    //     i++;
    // }
    // if (ans > INT32_MAX && ans < INT32_MIN)
    // {
    //     return 0;
    // }
    // cout << "Reverse of n is " << ans;

    // POWER OF TW0

    int n;
    cout << "Enter the Number you Want to convert\n";
    cin >> n;

    return 0;
}