#include <bits\stdc++.h>
using namespace std;
int main()
{
    // SYNTAX
    set<int> st;
    st.insert(4);
    st.insert(2);
    st.insert(2);
    st.insert(1);
    st.insert(3);

       return 0;
}